import { UserAccessDisabledDirective } from './user-access-disabled.directive';

describe('UserAccessDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAccessDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
