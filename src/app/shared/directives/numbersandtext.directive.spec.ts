import { NumbersandtextDirective } from './numbersandtext.directive';

describe('NumbersandtextDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersandtextDirective();
    expect(directive).toBeTruthy();
  });
});
