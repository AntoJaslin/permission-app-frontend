import { UseraccesspermissionDirective } from './useraccesspermission.directive';

describe('UseraccesspermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new UseraccesspermissionDirective();
    expect(directive).toBeTruthy();
  });
});
